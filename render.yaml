services:
  - type: web
    name: analizador-esofago
    env: python
    buildCommand: pip install --upgrade pip && pip install -r requirements.txt
    startCommand: python -m gunicorn app:app
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.11

